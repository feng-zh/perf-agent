<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.hp.ts.perf.agent</groupId>
	<artifactId>exception-source</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<properties>
		<maven.compiler.source>1.6</maven.compiler.source>
		<maven.compiler.target>1.6</maven.compiler.target>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>
	<build>
		<finalName>${project.artifactId}</finalName>
		<plugins>
			<plugin>
				<artifactId>maven-shade-plugin</artifactId>
				<version>2.1</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<dependencyReducedPomLocation>${project.build.directory}/dependency-reduced-pom.xml</dependencyReducedPomLocation>
							<relocations>
								<relocation>
									<pattern>org.objectweb.asm</pattern>
									<shadedPattern>com.hp.ts.perf.agent.org.objectweb.asm</shadedPattern>
								</relocation>
							</relocations>
							<artifactSet>
								<includes>
									<include>org.ow2.asm:*</include>
								</includes>
							</artifactSet>
							<filters>
								<filter>
									<artifact>${project.groupId}:${project.artifactId}</artifact>
									<excludes>
										<exclude>**/shared/**</exclude>
										<exclude>**/test/**</exclude>
									</excludes>
								</filter>
							</filters>
							<transformers>
								<transformer
									implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<manifestEntries>
										<Premain-Class>com.hp.ts.perf.agent.expsrc.AgentMain</Premain-Class>
										<Agent-Class>com.hp.ts.perf.agent.expsrc.AgentMain</Agent-Class>
										<Can-Redefine-Classes>true</Can-Redefine-Classes>
										<Can-Retransform-Classes>true</Can-Retransform-Classes>
									</manifestEntries>
								</transformer>
							</transformers>
							<shadedArtifactAttached>true</shadedArtifactAttached>
              				<shadedClassifierName>agent</shadedClassifierName>
              				<finalName>${project.artifactId}-agent</finalName>
						</configuration>
					</execution>
					<execution>
						<id>boot-jar</id>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<dependencyReducedPomLocation>${project.build.directory}/dependency-reduced-boot-pom.xml</dependencyReducedPomLocation>
							<relocations>
								<relocation>
									<pattern>com.strobel</pattern>
									<shadedPattern>com.hp.ts.perf.agent.com.strobel</shadedPattern>
								</relocation>
							</relocations>
							<artifactSet>
								<includes>
									<include>org.bitbucket.mstrobel:*</include>
									<include>${project.groupId}:${project.artifactId}</include>
								</includes>
							</artifactSet>
							<filters>
								<filter>
									<artifact>${project.groupId}:${project.artifactId}</artifact>
									<includes>
										<include>**/shared/**</include>
									</includes>
								</filter>
							</filters>
							<shadedArtifactAttached>true</shadedArtifactAttached>
              				<shadedClassifierName>boot</shadedClassifierName>
              				<finalName>${project.artifactId}-boot</finalName>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>org.ow2.asm</groupId>
			<artifactId>asm-debug-all</artifactId>
			<version>4.1</version>
		</dependency>
		<dependency>
			<groupId>org.bitbucket.mstrobel</groupId>
			<artifactId>procyon-compilertools</artifactId>
			<version>0.4.2</version>
		</dependency>
	</dependencies>
</project>
